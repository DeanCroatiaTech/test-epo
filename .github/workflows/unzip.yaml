on:
  push: {}
  workflow_dispatch: {}
  pull_request:
    types: [ opened, reopened, synchronize ]

jobs:
  unzip:
    runs-on:
    - self-hosted
    - default
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Extract some files
      run: 7z x internalportal-report.zip
  

